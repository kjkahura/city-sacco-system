# Postgres
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=sacco
PGPOOL_MAX=20

# Required in production. Generate with: openssl rand -base64 48
JWT_SECRET=

# Set true only when a trusted load balancer sets X-Forwarded-Host.
TRUST_PROXY=false

PORT=4000
NODE_ENV=development
